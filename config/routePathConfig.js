const routePath = {
    'index': '../routes/index',
    'users': '../routes/users',
    'register': '../routes/register',
    'login': '../routes/login',
    'jwtToken': '../routes/api/jwtToken',
}

module.exports = routePath;